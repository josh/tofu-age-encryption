env AGE_IDENTITY_FILE=/tmp/missing

stdin input.json
! exec tofu-age-encryption --decrypt
cmp stdout stdout.json
cmp stderr stderr.txt

-- input.json --
{"payload":"c2VjcmV0"}

-- stdout.json --
-- stderr.txt --
failed to read identity file "/tmp/missing": open /tmp/missing: no such file or directory
